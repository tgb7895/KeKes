<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a37046.foods">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LaucherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.StoreDetailsActivity" />
        <activity android:name=".activity.MenudetailsActivity" />
        <activity android:name=".activity.PlaceOrderActivity" />
        <activity android:name=".activity.AllUserCommentActivity" />
        <activity android:name=".activity.AllUserOrderActivity" />
        <activity android:name=".activity.ModifyPersonalInformation"></activity>
    </application>

</manifest>